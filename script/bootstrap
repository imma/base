#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  (cd "$shome/../go" && block bootstrap)
  (cd "$shome/../fogg-cli" && make)
  (cd "$shome/../go" && clean -fdx)
}

main "$@"
